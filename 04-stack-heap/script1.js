//Values are stored in a stack

const name='arun';
const age=18;
console.log(name, age);

//REFERENCE VARIABLE stored in the heap

const person={
    name:'kaithi',
    age:21,
};
let lastperson=person;
lastperson.name='vetrivel';
console.log(lastperson);

